{
  "name": "dynasty-league",
  "version": "1.0.0",
  "private": true,
  "description": "Dynasty League - Free-to-Play Basketball Franchise Simulator",
  "scripts": {
    "dev": "cd frontend && npx vite --host",
    "backend": "cd backend && node server.js",
    "build": "cd frontend && npx vite build",
    "preview": "cd frontend && npx vite preview",
    "deploy": "cd frontend && npx vercel --prod"
  },
  "dependencies": {
    "axios": "^1.17.0",
    "cheerio": "^1.2.0",
    "uuid": "^14.0.0"
  }
}
